apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
secretGenerator:
  - name: app-secrets
    namespace: k3s-auth
    envs:
      - secret.env
generatorOptions:
  disableNameSuffixHash: true
